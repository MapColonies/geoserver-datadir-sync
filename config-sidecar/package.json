{
  "name": "geoserver-config-sidecar",
  "version": "1.0.0",
  "description": "This container fetches config for geoserver, and triggers a config reload",
  "main": "index.js",
  "engines": {
    "node": ">=14.8.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "geoserver",
    "zx",
    "openshift",
    ""
  ],
  "author": "CptSchnitz",
  "license": "ISC",
  "dependencies": {
    "zx": "^1.14.1",
    "@aws-sdk/client-s3": "^3.18.0"
  },
  "devDependencies": {
    "@aws-sdk/types": "^3.18.0"
  }
}
